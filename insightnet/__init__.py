"""InsightNet data collection and dashboard helpers."""

# The single source of truth for this project's version. `pyproject.toml` reads this
# attribute, the GitHub Pages deployment stamps it into the site footer, and every
# release is tagged `v<version>`. Read the "Versioning" section of README.md before
# changing it: every change ships with a bump, and which digit moves is not arbitrary.
__version__ = "1.1.2"
